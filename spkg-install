if [ "$SAGE_LOCAL" = "" ]; then
   echo "SAGE_LOCAL undefined ... exiting";
   echo "Maybe run 'femhub -sh'?"
   exit 1
fi

if [ "$SAGE_ROOT" = "" ]; then
   echo "SAGE_ROOT undefined ... exiting";
   echo "Maybe run 'femhub -sh'?"
   exit 1
fi

python setup.py install
